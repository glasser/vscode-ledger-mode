<!DOCTYPE html>
<html>
<head>
    <title>{{title}}</title>
    <style>
        body { 
            font-family: Arial, sans-serif; 
            margin: 20px; 
            line-height: 1.4;
            background-color: #ffffff;
            color: #333333;
        }
        .test-case { 
            margin-bottom: 40px;
            border: 1px solid #ddd;
            border-radius: 8px;
            overflow: hidden;
        }
        h2 {
            background-color: #f5f5f5;
            padding: 15px;
            margin: 0;
            border-bottom: 1px solid #ddd;
        }
        .description {
            padding: 15px;
            margin: 0;
            background-color: #e8f4f8;
            border-bottom: 1px solid #ddd;
        }
        .target-date {
            padding: 0 15px 15px 15px;
            background-color: #e8f4f8;
            margin: 0;
        }
        .test-row { 
            display: flex;
        }
        .test-input, .test-output { 
            flex: 1; 
            padding: 15px;
        }
        .test-input { 
            background-color: #fafafa; 
            border-right: 1px solid #ddd;
        }
        .test-output { 
            background-color: #f0f8f0; 
        }
        h3 {
            font-weight: bold;
            margin: 0 0 10px 0;
            color: #666;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        pre {
            background-color: #f8f8f8;
            padding: 12px;
            border-radius: 4px;
            border: 1px solid #e0e0e0;
            font-family: 'Courier New', monospace;
            font-size: 13px;
            line-height: 1.4;
            margin: 0;
            overflow-x: auto;
        }
        code {
            font-family: inherit;
        }
        h1 {
            color: #333;
            border-bottom: 2px solid #0066cc;
            padding-bottom: 10px;
        }
        .summary {
            background-color: #e8f4f8;
            padding: 15px;
            border-radius: 6px;
            margin-bottom: 30px;
            border-left: 4px solid #0066cc;
        }
    </style>
</head>
<body>
    <h1>{{title}}</h1>
    
    <div class="summary">
        <p>Tests for generating recurring transactions from RECUR comments. Each test shows how transactions with <code>; RECUR:period</code> comments are expanded up to a target date.</p>
        <p><strong>Test cases:</strong> {{totalTestCases}}</p>
    </div>
    
    {{#each testCases}}
    <div class="test-case">
        <h2>{{name}}</h2>
        <p class="description">{{config.description}}</p>
        <p class="target-date">Target Date: <code>{{config.targetDate}}</code></p>
        
        <div class="test-row">
            <div class="test-input">
                <h3>Input</h3>
                <pre><code>{{{escapeHtml input}}}</code></pre>
            </div>
            
            <div class="test-output">
                <h3>Expected Output</h3>
                <pre><code>{{{escapeHtml expected}}}</code></pre>
            </div>
        </div>
    </div>
    {{/each}}
</body>
</html>